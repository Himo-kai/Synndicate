{
  "name": "synndicate-ci",
  "version": "1.0.0",
  "description": "CI/CD configuration for Synndicate AI Platform",
  "private": true,
  "scripts": {
    "danger": "danger ci --use=ts-node --dangerfile=dangerfile.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "danger": "^11.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
